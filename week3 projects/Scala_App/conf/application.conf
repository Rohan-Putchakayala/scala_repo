# =============================
# Application Configuration
# =============================

play.http.secret.key = "changeme"
play.http.secret.key = ${?APPLICATION_SECRET}

# Allowed Hosts
play.filters.hosts {
  allowed = ["."]
}

# =============================
# Database Configuration (MySQL)
# =============================

slick.dbs.default.profile = "slick.jdbc.MySQLProfile$"
slick.dbs.default.db.driver = "com.mysql.cj.jdbc.Driver"

slick.dbs.default.db.url = "jdbc:mysql://azuremysql8823.mysql.database.azure.com:3306/Rohan_Putchakayala?useSSL=false&serverTimezone=UTC&connectionTimeZone=UTC"
slick.dbs.default.db.user = "mysqladmin"
slick.dbs.default.db.password = "Password@12345"

# Evolutions
play.evolutions.autoApply = true
play.evolutions.autoApplyDowns = false

# =============================
# (Optional) H2 Local Database
# =============================
# slick.dbs.default.profile = "slick.jdbc.H2Profile$"
# slick.dbs.default.db.driver = "org.h2.Driver"
# slick.dbs.default.db.url = "jdbc:h2:mem:visitor_management;DB_CLOSE_DELAY=-1"
# slick.dbs.default.db.user = ""
# slick.dbs.default.db.password = ""

# =============================
# Kafka Configuration
# =============================

kafka.bootstrap.servers = "localhost:9092"

kafka.topics {
  host-notifications = "visitor-host-notifications"
  it-notifications = "visitor-it-notifications"
  security-notifications = "visitor-security-notifications"
  checkout-notifications = "visitor-checkout-notifications"
}

# =============================
# Email (SMTP)
# =============================

email.smtp.host = "smtp.gmail.com"
email.smtp.port = 587

# Gmail MUST use App Password (not normal password)
email.smtp.user = "rohanbold123@gmail.com"
email.smtp.password = "xikh mllb jqmj bdgk"

email.from = "rohanbold123@gmail.com"

# Override via environment variables (Production)
# email.smtp.user = ${?EMAIL_USER}
# email.smtp.password = ${?EMAIL_PASSWORD}

# =============================
# Akka Configuration
# =============================

akka {
  loglevel = "INFO"
  actor.default-dispatcher {
    fork-join-executor {
      parallelism-min = 8
      parallelism-factor = 2.0
      parallelism-max = 64
    }
  }
}

# =============================
# Play Modules
# =============================

play.modules.enabled += "modules.ActorModule"
