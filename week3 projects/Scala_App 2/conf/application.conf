# Application Configuration
play.http.secret.key = "changeme"
play.http.secret.key = ${?APPLICATION_SECRET}

# Allowed hosts filter
play.filters.hosts {
  allowed = ["."]
}

# Database configuration - Azure MySQL
slick.dbs.default.profile = "slick.jdbc.MySQLProfile$"
slick.dbs.default.db.driver = "com.mysql.cj.jdbc.Driver"
slick.dbs.default.db.url = "jdbc:mysql://azuremysql8823.mysql.database.azure.com:3306/Rohan_Putchakayala?useSSL=false&serverTimezone=UTC&connectionTimeZone=UTC"
slick.dbs.default.db.user = "mysqladmin"
slick.dbs.default.db.password = "Password@12345"
db.default.driver = "com.mysql.cj.jdbc.Driver"
db.default.url = "jdbc:mysql://azuremysql8823.mysql.database.azure.com:3306/Rohan_Putchakayala?useSSL=false&serverTimezone=UTC&connectionTimeZone=UTC"
db.default.username = "mysqladmin"
db.default.password = "Password@12345"

# Auto-apply database evolutions (for development)
play.evolutions.autoApply=true
play.evolutions.autoApplyDowns=false
play.evolutions.db.default.enabled = true
play.evolutions.db.default.autoApply = true
play.evolutions.enabled = true

# For local development with H2 (commented out):
# slick.dbs.default.profile = "slick.jdbc.H2Profile$"
# slick.dbs.default.db.driver = "org.h2.Driver"
# slick.dbs.default.db.url = "jdbc:h2:mem:room_management;DB_CLOSE_DELAY=-1"
# slick.dbs.default.db.user = ""
# slick.dbs.default.db.password = ""

# Kafka Configuration
kafka.bootstrap.servers = "localhost:9092"
kafka.enabled = false
# Topics are defined and used directly in code for room management:
# - "room-preparation-notifications"
# - "room-release-notifications"
# - "admin-notifications"

# Email Configuration (SMTP)
email.smtp.host = "smtp.gmail.com"
email.smtp.port = 587
# IMPORTANT: Use the SAME Gmail address for smtp.user and email.from
# This account MUST have a Gmail App Password configured (normal password will NOT work).
# For local testing you can hard-code these values; in production prefer env overrides.
email.smtp.user = "rohanbold123@gmail.com"        # e.g. my.company.app@gmail.com
email.smtp.password = "xikh mllb jqmj bdgk"         # Gmail App Password, NOT your normal password
email.from = "rohanbold123@gmail.com"             # Must match email.smtp.user

# Optional: override via environment variables in production
# email.smtp.user = ${?EMAIL_USER}
# email.smtp.password = ${?EMAIL_PASSWORD}

# Akka Configuration
akka {
  loglevel = "INFO"
  actor {
    default-dispatcher {
      fork-join-executor {
        parallelism-min = 8
        parallelism-factor = 2.0
        parallelism-max = 64
      }
    }
  }
}

# Play Modules
play.modules.enabled += "modules.ActorModule"
